on: pull_request
name: Pull Request
jobs:
  test:
    name: Testing
    runs-on: ubuntu-latest
    steps:
      - name: Test
        # No alpine, lacking cgo support
        uses: docker://golang:1.13.1
        with:
          entrypoint: sh
          args: .github/run-tests.sh