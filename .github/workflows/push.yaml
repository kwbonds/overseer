on: push
name: Push Event
jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Test
        uses: golang:1.13.1-alpine3.10
        args: bash .github/run-tests.sh